<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.acer.geomudah">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".secondActivity" />
        <activity android:name=".PrimerActivity" />
        <activity android:name=".TertierActivity" />
        <activity android:name=".PelanconganActivity" />
        <activity android:name=".EkologiActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".TasikTemenggorMapsActivity"
            android:label="@string/title_activity_tasik_temenggor_maps" />
        <activity android:name=".KesihatanActivity" />
        <activity
            android:name=".HospitalSungaiBulohMapsActivity"
            android:label="@string/title_activity_hospital_sungai_buloh_maps" />
        <activity android:name=".PendidikanActivity" />
        <activity
            android:name=".UIAMapsActivity"
            android:label="@string/title_activity_uiamaps" />
        <activity android:name=".BudayaActivity" />
        <activity
            android:name=".KotaAFamosaMapsActivity"
            android:label="@string/title_activity_kota_afamosa_maps" />
        <activity android:name=".SukanActivity" />
        <activity
            android:name=".SICMapsActivity"
            android:label="@string/title_activity_sicmaps" />
        <activity android:name=".MICEActivity" />
        <activity
            android:name=".PWTCMapsActivity"
            android:label="@string/title_activity_pwtcmaps" />
        <activity android:name=".TemaActivity" />
        <activity
            android:name=".GeorgetownMapsActivity"
            android:label="@string/title_activity_georgetown_maps" />
        <activity android:name=".PertanianActivity" />
        <activity
            android:name=".CameronHighlandMapsActivity"
            android:label="@string/title_activity_cameron_highland_maps" />
        <activity android:name=".RekreasiActivity" />
        <activity
            android:name=".PulauSipadanMapsActivity"
            android:label="@string/title_activity_pulau_sipadan_maps" />
        <activity android:name=".SekunderActivity"></activity>
    </application>

</manifest>